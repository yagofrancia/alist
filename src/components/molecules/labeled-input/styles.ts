import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  root: {
    marginBottom: 8,
  },
  label: {
    paddingBottom: 3,
  },
  error: {
    marginLeft: 5,
  },
  errorPlaceholder: {
    height: 20,
  },
});

export default styles;
